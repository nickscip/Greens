import React from 'react';
import {Image} from 'react-native';

function Logo() {
    return (
        <Image
        source={require('../../assets/SMARTAPP.png')}
        />
    )
}

export default Logo;